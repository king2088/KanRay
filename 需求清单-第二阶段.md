# 看板管理低代码系统 — 第二阶段需求清单

> 技术栈：**ExpressJS 5**（5.2.x，Node.js 18+）+ Vue 3 + Element Plus 前后端分离
> 目标定位：在第一阶段（单用户 + Excel）基础上，升级为**多用户 + 角色权限（RBAC）+ 多数据库连接**的完整产品化 BI 平台
> 参考对标：Apache Superset 的数据库接入、语义层、RBAC、行级权限、SQL Lab、缓存、告警等高级能力

---

## 一、阶段目标与本阶段增量

第二阶段把系统从"单机个人工具"升级为"**可面向外部客户交付的多用户企业级 BI**"。在第一阶段已建好的 **数据集/图表/看板** 四层模型和 **DataProvider 统一数据访问抽象**之上，重点补齐三类能力：

1. **数据接入面拓宽**：从"仅 Excel"扩展到"任意 SQL 数据库 + 文件"，并通过统一的语义层管理
2. **多用户与安全**：注册/登录、角色权限（RBAC）、行级权限（RLS）、数据源级权限
3. **企业级增值能力**：SQL 查询（对标 Superset SQL Lab）、缓存、告警报表、API、嵌入、审计，为产品化/商业化铺路

> 本阶段严格建立在第一阶段架构之上。第一阶段未做好的抽象（尤其是 DataProvider 与实体所有权归属）是本阶段能否顺利扩展的前提。

---

## 二、整体架构（第二阶段完整形态）

```
┌────────────────────────────────────────────────────────────────┐
│  前端（Vue 3 + Element Plus + Pinia + Vite）                     │
│  ┌────────────┐ ┌──────────┐ ┌────────┐ ┌───────┐ ┌─────────┐  │
│  │ 数据源管理   │ │ 数据集    │ │ 图表    │ │ 看板   │ │ 管理后台 │  │
│  │ (多数据库)  │ │ (语义层)  │ │        │ │       │ │ 用户/角色│  │
│  └─────┬──────┘ └────┬─────┘ └───┬────┘ └───┬───┘ └────┬────┘  │
│        │             │           │           │          │        │
│  ▲  RESTful API + WebSocket(异步任务/结果推送)                   │
├──┼──────────────────────────────────────────────────────────────┤
│  后端（ExpressJS 5）                                              │
│  ┌────────────────────────────────────────────────────────────┐ │
│  │ 认证/授权模块   RBAC 角色权限   RLS 行级权限   审计日志       │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │ 数据集(语义层)   图表/看板      SQL Lab(Jinja)   虚拟数据集   │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │      统一 DataProvider 数据访问层（核心扩展点）              │ │
│  │  ┌─────────┐ ┌───────────┐ ┌────────────┐ ┌──────────────┐  │ │
│  │  │Excel    │ │MySQL       │ │PostgreSQL  │ │SQLServer等    │  │ │
│  │  │Provider │ │Provider    │ │Provider    │ │Provider      │  │ │
│  │  └─────────┘ └────────────┘ └────────────┘ └──────────────┘  │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │          查询引擎 + 缓存(Redis) + 异步任务队列(Celery式)     │ │
│  ├────────────────────────────────────────────────────────────┤ │
│  │         元数据库(PostgreSQL/MySQL) 存配置/图表/看板/用户      │ │
│  └────────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

### 架构关键变化（相对于第一阶段）
| 项 | 第一阶段 | 第二阶段 |
|----|----------|----------|
| 元数据存储 | SQLite/文件 | PostgreSQL 或 MySQL（多用户 + 权限 + 并发） |
| 缓存 | 内存缓存 | Redis（分布式、多实例共享） |
| 异步 | 同步/简单异步 | 任务队列（如 BullMQ）+ WebSocket 推送结果 |
| 数据源 | 仅 Excel | 多数据源连接池（支持多数据库） |
| 认证 | 无 | JWT + 刷新令牌（可扩展 SSO/OAuth/LDAP） |
| 权限 | 无 | RBAC + RLS + 资源所有权 |

---

## 三、功能需求模块

> 优先级：**P0** = 本阶段必须；**P1** = 应有；**P2** = 增强

### 3.1 多数据源接入（P0）— 对标 Superset"连接任意 SQL 数据库"

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| DB-01 | 数据源管理 CRUD [已实现 M2] | P0 | 添加/编辑/测试/删除/启停数据源连接 |
| DB-02 | 支持主流数据库连接 [已实现 M2] | P0 | MySQL、PostgreSQL、SQL Server（接入 Oracle、ClickHouse、SQLite 等按需扩展） |
| DB-03 | **连接测试** [已实现 M2] | P0 | 保存前测试连通性，显示成功/失败原因 |
| DB-04 | 连接配置管理 [已实现 M2] | P0 | 主机/端口/库名/账号/密码（密码加密存储）/连接池上限 |
| DB-05 | Schema / 表浏览 [已实现 M2] | P0 | 展开数据库 → schema → 表 → 字段（对标 Superset 的树形导航） |
| DB-06 | 安全连接（SSL/TLS 选项） | P1 | 支持 SSL 配置 |
| DB-07 | 数据源级权限控制 | P1 | 不同角色可访问的数据源不同 |
| DB-08 | 连接监控与状态展示 | P1 | 显示连接状态、最近测试时间、查询量 |

**验收要点**：管理员能添加一个 MySQL 数据源，测试通过后，普通用户即可基于其下的表创建数据集/图表。

---

### 3.2 数据集与语义层（P0）— 对标 Superset 的物理/虚拟数据集

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| DS-01 | **物理数据集**：从表/视图注册为数据集 | P0 | 把数据库中的表或视图声明为可分析的"数据集" |
| DS-02 | **虚拟数据集**：由 SQL 查询定义为数据集 | P0 | 对标 Superset：把一条已保存的 SQL 当"表"用，分析师无需建表权限 |
| DS-03 | 数据集字段元数据 | P0 | 字段类型、别名、注释、是否隐藏、是否为维度/指标 |
| DS-04 | **数据集级指标（计算指标）** | P0 | 用 SQL 表达式定义一次业务口径（如 `count(distinct user_id)`），所有图表复用 —— 对标 Superset 语义层核心价值 |
| DS-05 | 数据集目录/层级文件夹 | P1 | 对标 Superset 6.0 的层级文件夹系统，分组管理数据集 |
| DS-06 | 数据集权限控制 | P1 | 哪些角色可用哪些数据集 |
| DS-07 | 数据集级行级权限（RLS） | P0 | 见 3.5 安全 |
| DS-08 | 数据集缓存策略 | P1 | 为不同数据集设置不同缓存 TTL（快照表长缓存、近实时表短缓存） |
| DS-09 | 数据集重复/溯源管理 | P2 | 沿用第一阶段的 DataProvider 扩展 |

**验收要点**：建立一个"虚拟数据集"（一条 SQL），在其上定义一个计算指标（如 去重活跃用户），然后所有图表引用该指标得到一致结果，避免"各算各的数"。

---

### 3.3 多用户与认证（P0）

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| AU-01 | 用户注册 / 登录 / 登出 [已实现 M1] | P0 | 邮箱+密码；密码哈希存储（bcrypt） |
| AU-02 | JWT 鉴权 + 刷新令牌 [已实现 M1] | P0 | 访问令牌短期、刷新令牌长期，支持过期续期 |
| AU-03 | 个人中心 [已实现 M1] | P0 | 修改资料、改密码 |
| AU-04 | 密码找回 / 重置 | P1 | 邮箱验证码重置 |
| AU-05 | 会话管理（多端/失效） | P1 | 查看/踢出登录会话 |
| AU-06 | 第三方登录（OAuth/企业微信/GitHub） | P2 | 对标 Superset 的 OAuth/LDAP/SAML |
| AU-07 | 两步验证（2FA） | P2 | |

---

### 3.4 角色权限（RBAC）（P0）— 对标 Superset

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| RB-01 | 内置角色体系 [已实现 M1] | P0 | 至少四类：**管理员**（全部权限）、**数据工程师/分析师**（管理数据源、数据集、SQL）、**看板编辑者**（建图排版）、**查看者**（只看） |
| RB-02 | 自定义角色 [已实现 M1] | P1 | 创建/编辑角色，勾选权限点组合 |
| RB-03 | **权限点（Permission）设计** [已实现 M1] | P0 | 细粒度权限：按模块（数据源/数据集/图表/看板/SQL/用户管理）× 操作（增/删/改/查） |
| RB-04 | 用户-角色分配 [已实现 M1] | P0 | 一个用户可拥有多个角色 |
| RB-05 | **资源所有权 + 共享** | P0 | 图表/看板/数据集归属创建者，支持共享给某角色/某用户（对标 Superset 的资源级访问） |
| RB-06 | 看板/图表级别访问控制 | P0 | 指定某看板可被哪些角色/用户查看或编辑 |
| RB-07 | 权限校验中间件 [已实现 M1] | P0 | Express 5 前端中间件统一校验资源权限与操作权限 |
| RB-08 | 团队/用户组（对标 Superset 6.0 用户组） | P2 | 角色挂到用户组，组分配用户 |

**验收要点**：一个"查看者"角色用户登录后，只能看到被共享的看板，无法编辑图表、无法访问数据源管理；尝试越权访问时后端返回 403。

---

### 3.5 行级权限（RLS）与数据安全（P0）

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| RL-01 | **行级权限规则配置** | P0 | 对标 Superset RLS：为数据集/角色配置附加 WHERE 条件，如"只看 region='华东' 的数据" |
| RL-02 | 规则作用域 | P0 | 规则按 角色/用户 组合定义，支持多规则叠加 |
| RL-03 | RLS 应用于所有查询 | P0 | 无论图表/SQL/导出，自动注入安全条件，杜绝绕过 |
| RL-04 | 敏感字段脱敏/隐藏 | P1 | 某些字段对某些角色隐藏或脱敏 |
| RL-05 | 数据导出脱敏 | P1 | 导出 CSV/图表时同样套用 RLS 与脱敏 |
| RL-06 | 访问审计日志 | P1 | 记录谁在何时对哪个数据集/看板做了什么 |

**验收要点**：同一张销售数据表，华东用户看到的只有华东数据，华南用户只有华南数据 —— 仅凭登录身份自动隔离，无需各自复制看板。

---

### 3.6 SQL Lab / SQL 查询（P0/P1）— 对标 Superset 的 SQL 编辑器

面向"分析师"高级用户，与小白无代码路径并存、互补。

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| SL-01 | SQL 编辑器（多 Tab） | P0 | 代码高亮、Schema/表自动补全 |
| SL-02 | Schema 树形浏览器 | P0 | 左侧展开表/字段/示例数据 |
| SL-03 | 异步查询执行 | P0 | 长查询后台执行，结果就绪推送/轮询，不阻塞浏览器（对标 Superset 异步执行） |
| SL-04 | 查询历史 / 已保存查询 | P1 | 历史记录、另存为保存查询、收藏 |
| SL-05 | **查询结果 → 一键生成图表** | P1 | 选取返回结果集转成图表/虚拟数据集（对标 Superset 一键 conversion） |
| SL-06 | 结果导出 CSV/Excel | P1 | |
| SL-07 | **Jinja 模板变量** | P2 | 在 SQL 中引用看板筛选、当前用户、时间范围（`{{ from_dttm }}` 等），实现交互式 SQL |
| SL-08 | SQL 方言格式化 | P2 | 按所选库方言格式化 SQL |
| SL-09 | SQL 执行权限控制 | P0 | 哪些角色可以用 SQL Lab（通常仅分析师以上，避免滥用） |
| SL-10 | 查询取消 / 超时 / 资源限制 | P1 | 支持取消查询、设置超时与最大返回行数 |

---

### 3.7 图表 / 看板能力增强（P1/P2）

在第一阶段图表模块基础上增加：

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| V-01 | 扩充图表类型库 | P1 | 补全：透视表、热力图、箱线图、散点图、地理地图(Geo)、桑基图、组合时序图等 | 
| V-02 | 交叉筛选（Cross-filter） | P1 | 点击图表数据联动过滤看板其他图表 |
| V-03 | 下钻 Drill-to-detail / Drill-by | P2 | 查看图表背后明细或跳转交互 |
| V-04 | **看板嵌入（Embed）** | P1 | 对标 Superset：通过可配置 URL/Token 把看板嵌入外部系统（产品化关键能力） |
| V-05 | CSS 模板定制品牌化 | P2 | 看板级样式微调 |
| V-06 | 看板导出 PDF/截图 | P2 | |

---

### 3.8 缓存 / 性能 / 调度（P0/P1）

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| PC-01 | Redis 结果缓存 | P1 | 按查询指纹缓存结果，带 TTL，数据源联动失效 |
| PC-02 | 数据集独立缓存策略 | P1 | 不同数据集不同 TTL |
| PC-03 | 异步任务队列 | P1 | 长查询/大 Excel/批量渲染走任务队列（BullMQ），避免阻塞 |
| PC-04 | 看板/图表缩略图缓存 | P2 | 预热高频看板 |
| PC-05 | 并发 / 连接池管理 | P1 | 数据库连接池、查询并发上限、防止单一用户拖垮后端 |

---

### 3.9 告警与定时报表（P1/P2）— 对标 Superset Alert & Report

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| AR-01 | 阈值告警 | P1 | 配置指标超过/低于阈值时触发通知 |
| AR-02 | 定时看板快照发送 | P1 | 按计划把看板截图/报表通过邮件或 Webhook 发送（对标 Superset 定时快照） |
| AR-03 | 通知渠道 | P2 | 邮件 / 企业微信 / 钉钉 / Slack / Webhook |
| AR-04 | 告警历史与静默 | P2 | 记录触发历史，支持静默窗口 |

---

### 3.10 系统管理与审计（P1）

| 编号 | 需求 | 优先级 | 说明 |
|------|------|--------|------|
| AD-01 | 用户 / 角色管理后台 [已实现 M1] | P0 | 管理员界面管理用户、角色、权限 |
| AD-02 | 操作审计日志 | P1 | 登录、数据访问、看板变更、权限变更记录 |
| AD-03 | 系统配置项管理 | P1 | 缓存开关、上传限制、注册开关等可配置 |
| AD-04 | RESTful 开放 API | P2 | 对标 Superset REST API：程序化管理图表/看板/数据集（产品化对接核心） |
| AD-05 | 备份 / 恢复 | P1 | 元数据与配置备份，数据源连接信息（脱敏） |
| AD-06 | 多租户支持 | P2 | 面向外部客户的产品化终极形态（可选规划） |

---

## 四、安全设计要点

1. **密码安全**：bcrypt 加盐哈希，禁止明文
2. **令牌安全**：JWT 签名密钥环境变量注入；刷新令牌存储/吊销
3. **SQL 注入**：参数化查询；SQL Lab 需权限 + 只读账号连接
4. **连接信息加密**：数据库密码加密存储（如 AES，密钥在环境变量）
5. **越权防护**：后端资源级鉴权中间件，前端隐藏按钮只是体验，权限永远后端校验
6. **RLS 强制**：所有数据出口（图表/导出/SQL）统一经过 RLS 注入，不允许旁路
7. **文件安全**：延续第一阶段文件类型白名单，拒绝宏文件

---

## 五、数据模型（核心实体关系）

```
User 1───* UserRole *───1 Role 1───* Permission
User 1───* Ownership(资源所有者) *───1 {Dataset, Chart, Dashboard, DataSource}

DataSource 1───* Dataset(物理/虚拟) 1───* ChartMetric(计算指标)
Dataset 1───* Chart  *───* Dashboard
Dataset 1───* RLSRule *───1 Role
Role 1───* DashboardGrant(看板共享) 
```

- **阶段一兼容**：把第一阶段的"Excel 数据集"映射为 `DataSource.type = 'excel'` 的特殊数据源，完全复用第二阶段的数据集/图表/看板与权限模型
- **所有权**：所有资源都有 owner，配合共享授权实现 RB-05

---

## 六、优先实施顺序（Roadmap 建议）

| 里程碑 | 内容 |
|--------|------|
| **M1：多用户/认证基座** | 用户注册登录 JWT + RBAC 角色权限模型 + 管理员后台（P0 的 3.3/3.4） |
| **M2：数据库接入** | 统一 DataProvider + MySQL/PostgreSQL Provider + Schema 浏览（P0 的 3.1/3.2） |
| **M3：语义层 + 权限落地** | 物理/虚拟数据集 + 计算指标 + 数据集/资源级权限（3.2/3.4 完善） |
| **M4：RLS 行级权限 + 审计** | 行级权限注入所有查询出口（3.5） |
| **M5：SQL Lab + 异步 + 缓存** | SQL 编辑器一体化成图 + 任务队列 + Redis 缓存（3.6/3.8） |
| **M6：企业增强** | 嵌入、告警报表、开放 API、CSS/多租户（P1/P2） |

---

## 七、验收标准（Definition of Done）

1. 多用户可注册登录，通过 RBAC 角色权限控制数据源/数据集/图表/看板/管理功能，越权返回 403
2. 支持 MySQL + PostgreSQL 数据源连接，普通用户无需写 SQL 即可基于表或虚拟数据集建图排版
3. 行级权限（RLS）在图表、导出、SQL 所有数据出口强制生效，多租户数据隔离验证通过
4. 数据访问完全通过统一 DataProvider，图表/看板层与底层数据源解耦
5. 长查询异步执行 + Redis 缓存，看板打开体验流畅（P95 < 1s，缓存命中时）
6. 所有安全项（密码、token、连接加密、SQL 注入、文件）通过基本安全审查

---

## 八、与第一阶段的关系总结

| 维度 | 第一阶段 | 第二阶段（本文件） |
|------|----------|---------------------|
| 用户 | 单用户 | 多用户 + RBAC + RLS |
| 数据源 | 仅 Excel | Excel + MySQL/PostgreSQL/SQL Server 等 |
| 数据模型 | 保留 | 完全复用并扩展（DataSource.type 区分） |
| 存储 | SQLite/文件 | PostgreSQL/MySQL + Redis |
| 核心价值 | 小白快速建看板 | 企业级安全 + 多数据源 + 高级分析 |

> **迁移保障**：第一阶段若严格落地"统一 DataProvider 抽象"与"实体带 owner 预留"两点，第二阶段升级将是对现有数据的平滑扩展，而非推倒重来。这是两个阶段文档共同强调的最关键架构约束。
